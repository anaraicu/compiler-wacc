# simple for loop

# Output:
# #prints from 1 to 100 and the sum from 1 to 100#

# Program:

begin
  int s = 0 ;
  for (int i = 0; i < 100; i = i + 1)
    println i + 1 ; 
    s = s + i
  done ;
  println s
end